{% extends 'base.html.twig' %}

{% block body %}
    <h1>All Posts</h1>

    <div class="row">
        {% for post in posts %}
            <div class="col-md-3">
                {{ post.title }}
            </div>
        {% endfor %}
    </div>
{% endblock %}